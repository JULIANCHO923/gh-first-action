name: Custom Action 
on: [push]
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Run My Custom Action
        uses: ./greeting-action
        with:
          greeting: 'Hello Yulian'