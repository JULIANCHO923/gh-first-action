name: Custom Action 
on: [push]
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run My Custom Action
        uses: actions/greeting-action@1.0.0
        with:
          greeting: 'Hello Yulian'
